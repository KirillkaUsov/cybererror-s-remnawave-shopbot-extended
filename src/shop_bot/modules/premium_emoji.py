"""Премиум-эмодзи в сообщениях бота.

Никаких новых значков в текстах не появляется — мы подменяем те, что бот
и так шлёт. У обладателя Premium они оживают, у остальных остаётся ровно
тот же символ, что и раньше: он и есть содержимое тега <tg-emoji>, а
Telegram показывает его всем, кому анимация недоступна.

Отсюда и способ подключения: не переписывать полсотни строк с текстами, а
один раз пройтись по исходящему сообщению. Подмена живёт в middleware
сессии бота, поэтому новые тексты получают премиум-эмодзи сами собой.

Набор подобран вручную. Где есть «адаптивный» вариант — берём его: такие
эмодзи перекрашиваются под цвет интерфейса и в интерфейсе выглядят своими,
а не наклейкой поверх.
"""

from __future__ import annotations

import re

# Плоский эмодзи → id премиум-версии. Ключ пишем без селектора начертания
# (U+FE0F): в текстах он то есть, то нет, а искать надо одинаково.
EMOJI: dict[str, str] = {
    "⚠": "5422699448776021818",   # adaptive
    "✅": "5415824455970620428",   # adaptive
    "❌": "5210952531676504517",   # news
    "🎁": "5208508553551179849",   # adaptive
    "🎟": "5377599075237502153",
    "💳": "5422735818559086670",   # adaptive
    "📱": "5407025283456835913",
    "💰": "5375296873982604963",
    "🤝": "5357080225463149588",
    "🎉": "5461151367559141950",   # news
    "🛒": "5422468121837461796",   # adaptive
    "🔄": "5375338737028841420",   # news
    "💎": "5260745045316755375",   # adaptive
    "🏠": "5416041192905265756",   # news
    "🔑": "5330115548900501467",
    "⭐": "5422505599722085844",   # adaptive
    "🚀": "5445284980978621387",
    "⏳": "5451732530048802485",
    "🔔": "5458603043203327669",   # news
    "👤": "5373012449597335010",
    "📊": "5422871492281001864",   # adaptive
    "🌐": "5447410659077661506",   # news
    "⚡": "5456140674028019486",   # news
    "🔒": "5296369303661067030",   # news
    "💬": "5443038326535759644",   # news
    "🎯": "5350460637182993292",
    "🔥": "5353079703945120148",   # adaptive
}

VARIATION = "️"

# Внутрь тегов, кода и уже готовых <tg-emoji> не лезем: там подмена либо
# сломает разметку, либо обернёт эмодзи второй раз.
_PROTECTED = re.compile(
    r"(<tg-emoji\b.*?</tg-emoji>|<code\b.*?</code>|<pre\b.*?</pre>|<[^>]*>)",
    re.DOTALL | re.IGNORECASE,
)
_TARGETS = re.compile("(" + "|".join(re.escape(k) for k in EMOJI) + ")" + VARIATION + "?")

# Разметка длиннее текста примерно на 40 символов за каждый эмодзи, а у
# Telegram свой предел. Слишком длинное сообщение оставляем как есть —
# лучше без анимации, чем совсем без сообщения.
MAX_TEXT = 3500
MAX_CAPTION = 850


def _swap(match: re.Match) -> str:
    plain = match.group(0)
    emoji_id = EMOJI[match.group(1)]
    return f'<tg-emoji emoji-id="{emoji_id}">{plain}</tg-emoji>'


def upgrade(text: str | None, limit: int = MAX_TEXT) -> str | None:
    """Оживляет знакомые эмодзи. Незнакомые и любую разметку не трогает."""
    if not text or len(text) > limit or "<tg-emoji" in text:
        return text
    parts = _PROTECTED.split(text)
    # Нечётные куски — защищённые, их пропускаем нетронутыми.
    for i in range(0, len(parts), 2):
        parts[i] = _TARGETS.sub(_swap, parts[i])
    result = "".join(parts)
    return result if len(result) <= 4096 else text


def enabled(get_setting) -> bool:
    value = (get_setting("premium_emoji_enabled") or "true").strip().lower()
    return value not in ("0", "false", "off", "no")
